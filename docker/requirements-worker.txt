# ============================================================
#  requirements-worker.txt — dependencias do Celery worker
#  Inclui tudo do API + pacotes de processamento de audio/ML.
# ============================================================

# Herda todas as dependencias da API
-r requirements-api.txt

# PyTorch CPU (pinado na 2.1.x — versoes >= 2.4 do torchaudio exigem
# torchcodec que o Demucs nao instala automaticamente)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu
torchaudio==2.1.2+cpu

# Audio Processing
librosa==0.10.0
numpy==1.25.2
scipy==1.11.4
soundfile==0.12.1
pydub==0.25.1

# Source separation (requer torch)
demucs==4.0.1

# MIDI Processing
mido==1.3.0
pretty_midi==0.2.11

# MIDI extraction from audio (requer torch/tensorflow internamente)
basic-pitch==0.4.0
