[pytest]
markers =
    integration: testes de integração que dependem de recursos externos ao processo de teste (binário do LilyPond instalado, ficheiros de áudio reais, storage remoto). Corre-os isoladamente com `-m integration`; a suite normal usa `-m "not integration"`.

asyncio_mode = strict

testpaths = tests
